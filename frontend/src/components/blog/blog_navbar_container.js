import { connect } from 'react-redux';

import BlogNavbar from './blog_navbar';

const msp = state => {
    return {
        
    };
};

const mdtp = dispatch => {
    return {

    };
};

export default connect(
    msp,
    mdtp
)(BlogNavbar);